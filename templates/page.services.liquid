<div class="page-header">
  <div class="container">
    <nav class="breadcrumb">
      <a href="/">Home</a>
      <span>/</span>
      <span>Services</span>
    </nav>
    <h1 class="page-title">Our Services</h1>
    <p class="page-description">
      Professional services tailored to meet your unique needs. From personalized consultations to custom solutions, we deliver exceptional results.
    </p>
  </div>
</div>

<section class="services-section">
  <div class="container">
    <!-- Services Grid -->
    <div class="services-grid">
      <!-- Service 1 -->
      <div class="service-card card">
        <div class="service-card__image">
          <img src="{{ 'service-consultation.jpg' | asset_url }}" alt="Personal Consultation">
        </div>
        <div class="service-card__content card__content">
          <div class="service-card__category">Consultation</div>
          <h3 class="service-card__title">Personal Consultation</h3>
          <p class="service-card__description">
            One-on-one consultation with our experts to understand your needs and provide personalized recommendations.
          </p>
          <div class="service-card__details">
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
              <span>60 minutes</span>
            </div>
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <circle cx="12" cy="12" r="6"/>
                <line x1="12" y1="6" x2="12" y2="18"/>
              </svg>
              <span>In-person or virtual</span>
            </div>
          </div>
          <div class="service-card__pricing">
            <span class="price">$150</span>
            <span class="price-unit">/ session</span>
          </div>
        </div>
        <div class="service-card__actions">
          <button class="btn btn-primary btn--full book-service-btn" data-service="personal-consultation">
            Book Now
          </button>
        </div>
      </div>

      <!-- Service 2 -->
      <div class="service-card card">
        <div class="service-card__image">
          <img src="{{ 'service-custom-design.jpg' | asset_url }}" alt="Custom Design">
        </div>
        <div class="service-card__content card__content">
          <div class="service-card__category">Design</div>
          <h3 class="service-card__title">Custom Design Service</h3>
          <p class="service-card__description">
            Bespoke design solutions created specifically for your requirements. From concept to completion.
          </p>
          <div class="service-card__details">
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
              <span>2-4 weeks</span>
            </div>
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14,2 14,8 20,8"/>
              </svg>
              <span>3 revisions included</span>
            </div>
          </div>
          <div class="service-card__pricing">
            <span class="price">Starting at $500</span>
          </div>
        </div>
        <div class="service-card__actions">
          <button class="btn btn-primary btn--full book-service-btn" data-service="custom-design">
            Get Quote
          </button>
        </div>
      </div>

      <!-- Service 3 -->
      <div class="service-card card">
        <div class="service-card__image">
          <img src="{{ 'service-maintenance.jpg' | asset_url }}" alt="Maintenance & Support">
        </div>
        <div class="service-card__content card__content">
          <div class="service-card__category">Support</div>
          <h3 class="service-card__title">Maintenance & Support</h3>
          <p class="service-card__description">
            Ongoing support and maintenance services to keep your solutions running smoothly and efficiently.
          </p>
          <div class="service-card__details">
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
              <span>Monthly or annual</span>
            </div>
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
              </svg>
              <span>24/7 support</span>
            </div>
          </div>
          <div class="service-card__pricing">
            <span class="price">$99</span>
            <span class="price-unit">/ month</span>
          </div>
        </div>
        <div class="service-card__actions">
          <button class="btn btn-primary btn--full book-service-btn" data-service="maintenance-support">
            Subscribe
          </button>
        </div>
      </div>

      <!-- Service 4 -->
      <div class="service-card card">
        <div class="service-card__image">
          <img src="{{ 'service-training.jpg' | asset_url }}" alt="Training & Workshops">
        </div>
        <div class="service-card__content card__content">
          <div class="service-card__category">Education</div>
          <h3 class="service-card__title">Training & Workshops</h3>
          <p class="service-card__description">
            Educational sessions and workshops to help you make the most of our products and services.
          </p>
          <div class="service-card__details">
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
              <span>2-4 hours</span>
            </div>
            <div class="service-detail">
              <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                <circle cx="9" cy="7" r="4"/>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
              </svg>
              <span>Group or individual</span>
            </div>
          </div>
          <div class="service-card__pricing">
            <span class="price">$250</span>
            <span class="price-unit">/ session</span>
          </div>
        </div>
        <div class="service-card__actions">
          <button class="btn btn-primary btn--full book-service-btn" data-service="training-workshop">
            Book Session
          </button>
        </div>
      </div>
    </div>

    <!-- How It Works -->
    <section class="how-it-works">
      <h2 class="section-title">How It Works</h2>
      <div class="process-steps">
        <div class="process-step">
          <div class="process-step__number">1</div>
          <h3 class="process-step__title">Choose Service</h3>
          <p class="process-step__description">Select the service that best fits your needs</p>
        </div>
        <div class="process-step">
          <div class="process-step__number">2</div>
          <h3 class="process-step__title">Book Appointment</h3>
          <p class="process-step__description">Schedule a convenient time for your service</p>
        </div>
        <div class="process-step">
          <div class="process-step__number">3</div>
          <h3 class="process-step__title">Get Started</h3>
          <p class="process-step__description">Begin your journey with our expert guidance</p>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <h2 class="section-title">Frequently Asked Questions</h2>
      <div class="faq-list">
        <div class="faq-item">
          <button class="faq-question">
            <span>How do I book a service?</span>
            <svg class="faq-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6,9 12,15 18,9"/>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Simply click the "Book Now" button on any service card, fill out the booking form with your preferred date and time, and we'll confirm your appointment via email.</p>
          </div>
        </div>

        <div class="faq-item">
          <button class="faq-question">
            <span>Can I reschedule or cancel my booking?</span>
            <svg class="faq-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6,9 12,15 18,9"/>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Yes, you can reschedule or cancel your booking up to 24 hours in advance through your account dashboard or by contacting our support team.</p>
          </div>
        </div>

        <div class="faq-item">
          <button class="faq-question">
            <span>Do you offer virtual consultations?</span>
            <svg class="faq-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6,9 12,15 18,9"/>
            </svg>
          </button>
          <div class="faq-answer">
            <p>Absolutely! Many of our services are available virtually through video conferencing platforms for your convenience.</p>
          </div>
        </div>

        <div class="faq-item">
          <button class="faq-question">
            <span>What payment methods do you accept?</span>
            <svg class="faq-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6,9 12,15 18,9"/>
            </svg>
          </button>
          <div class="faq-answer">
            <p>We accept all major credit cards, PayPal, and bank transfers. Payment is required at the time of booking for most services.</p>
          </div>
        </div>
      </div>
    </section>
  </div>
</section>

<!-- Booking Modal -->
<div class="booking-modal" id="bookingModal">
  <div class="booking-modal__overlay"></div>
  <div class="booking-modal__content">
    <div class="booking-modal__header">
      <h3 class="booking-modal__title">Book Your Service</h3>
      <button class="booking-modal__close" aria-label="Close modal">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="18" y1="6" x2="6" y2="18"/>
          <line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>

    <form class="booking-form" id="bookingForm">
      <div class="form-row">
        <div class="form-group">
          <label for="serviceType" class="form-label">Service Type</label>
          <select id="serviceType" name="service" class="form-select" required>
            <option value="">Select a service...</option>
            <option value="personal-consultation">Personal Consultation</option>
            <option value="custom-design">Custom Design Service</option>
            <option value="maintenance-support">Maintenance & Support</option>
            <option value="training-workshop">Training & Workshops</option>
          </select>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="bookingName" class="form-label">Full Name</label>
          <input type="text" id="bookingName" name="name" class="form-input" required>
        </div>
        <div class="form-group">
          <label for="bookingEmail" class="form-label">Email Address</label>
          <input type="email" id="bookingEmail" name="email" class="form-input" required>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="bookingPhone" class="form-label">Phone Number</label>
          <input type="tel" id="bookingPhone" name="phone" class="form-input" required>
        </div>
        <div class="form-group">
          <label for="bookingDate" class="form-label">Preferred Date</label>
          <input type="date" id="bookingDate" name="date" class="form-input" required>
        </div>
      </div>

      <div class="form-group">
        <label for="bookingMessage" class="form-label">Additional Notes</label>
        <textarea id="bookingMessage" name="message" class="form-textarea" rows="4" placeholder="Tell us more about your requirements..."></textarea>
      </div>

      <div class="booking-form__actions">
        <button type="button" class="btn btn-secondary booking-modal__cancel">Cancel</button>
        <button type="submit" class="btn btn-primary">Book Service</button>
      </div>
    </form>
  </div>
</div>

<style>
.services-section {
  padding: var(--spacing-3xl) 0;
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: var(--spacing-xl);
  margin-bottom: var(--spacing-3xl);
}

.service-card {
  height: 100%;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.service-card__image {
  width: 100%;
  height: 200px;
  overflow: hidden;
}

.service-card__image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform var(--transition-normal);
}

.service-card:hover .service-card__image img {
  transform: scale(1.05);
}

.service-card__category {
  display: inline-block;
  background-color: var(--color-accent);
  color: var(--color-text);
  padding: var(--spacing-xs) var(--spacing-sm);
  border-radius: var(--radius-sm);
  font-size: var(--font-size-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: var(--spacing-sm);
}

.service-card__title {
  font-size: var(--font-size-xl);
  margin-bottom: var(--spacing-sm);
}

.service-card__description {
  color: var(--color-text-light);
  line-height: 1.6;
  margin-bottom: var(--spacing-lg);
}

.service-card__details {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-lg);
}

.service-detail {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  font-size: var(--font-size-sm);
  color: var(--color-text-light);
}

.service-detail .icon {
  color: var(--color-primary);
  flex-shrink: 0;
}

.service-card__pricing {
  margin-top: auto;
  margin-bottom: var(--spacing-lg);
}

.price {
  font-size: var(--font-size-xl);
  font-weight: 700;
  color: var(--color-primary);
}

.price-unit {
  font-size: var(--font-size-base);
  font-weight: 400;
  color: var(--color-text-light);
}

.service-card__actions {
  padding: 0 var(--spacing-lg) var(--spacing-lg);
}

/* How It Works */
.how-it-works {
  text-align: center;
  margin-bottom: var(--spacing-3xl);
}

.section-title {
  font-size: var(--font-size-3xl);
  margin-bottom: var(--spacing-2xl);
}

.process-steps {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: var(--spacing-xl);
  max-width: 800px;
  margin: 0 auto;
}

.process-step {
  padding: var(--spacing-xl);
}

.process-step__number {
  width: 60px;
  height: 60px;
  background-color: var(--color-primary);
  color: white;
  border-radius: var(--radius-full);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-size-xl);
  font-weight: 700;
  margin: 0 auto var(--spacing-lg);
}

.process-step__title {
  font-size: var(--font-size-lg);
  margin-bottom: var(--spacing-sm);
}

.process-step__description {
  color: var(--color-text-light);
  line-height: 1.6;
}

/* FAQ Section */
.faq-section {
  margin-bottom: var(--spacing-3xl);
}

.faq-list {
  max-width: 800px;
  margin: 0 auto;
}

.faq-item {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  margin-bottom: var(--spacing-md);
  overflow: hidden;
}

.faq-question {
  width: 100%;
  padding: var(--spacing-lg);
  background: none;
  border: none;
  text-align: left;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: var(--font-size-base);
  font-weight: 600;
  transition: background-color var(--transition-fast);
}

.faq-question:hover {
  background-color: var(--color-bg-alt);
}

.faq-icon {
  transition: transform var(--transition-fast);
}

.faq-item.active .faq-icon {
  transform: rotate(180deg);
}

.faq-answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height var(--transition-normal);
  padding: 0 var(--spacing-lg);
  background-color: var(--color-bg-alt);
}

.faq-item.active .faq-answer {
  max-height: 200px;
  padding-bottom: var(--spacing-lg);
}

/* Booking Modal */
.booking-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  display: none;
  align-items: center;
  justify-content: center;
}

.booking-modal.active {
  display: flex;
}

.booking-modal__overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
}

.booking-modal__content {
  position: relative;
  background-color: white;
  border-radius: var(--radius-lg);
  width: 100%;
  max-width: 500px;
  max-height: 90vh;
  overflow-y: auto;
  margin: var(--spacing-md);
  box-shadow: var(--shadow-xl);
}

.booking-modal__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-xl);
  border-bottom: 1px solid var(--color-border);
}

.booking-modal__title {
  font-size: var(--font-size-xl);
  margin: 0;
}

.booking-modal__close {
  background: none;
  border: none;
  cursor: pointer;
  color: var(--color-text-light);
  padding: var(--spacing-sm);
  border-radius: var(--radius-md);
  transition: background-color var(--transition-fast);
}

.booking-modal__close:hover {
  background-color: var(--color-bg-alt);
}

.booking-form {
  padding: var(--spacing-xl);
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-md);
}

.form-textarea {
  resize: vertical;
  min-height: 100px;
}

.booking-form__actions {
  display: flex;
  gap: var(--spacing-md);
  justify-content: flex-end;
  margin-top: var(--spacing-xl);
  padding-top: var(--spacing-xl);
  border-top: 1px solid var(--color-border);
}

/* Responsive Design */
@media (max-width: 768px) {
  .services-grid {
    grid-template-columns: 1fr;
  }

  .process-steps {
    grid-template-columns: 1fr;
  }

  .form-row {
    grid-template-columns: 1fr;
  }

  .booking-form__actions {
    flex-direction: column;
  }

  .booking-form__actions .btn {
    width: 100%;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // FAQ Accordion
  const faqItems = document.querySelectorAll('.faq-item');

  faqItems.forEach(item => {
    const question = item.querySelector('.faq-question');

    question.addEventListener('click', function() {
      // Close other items
      faqItems.forEach(otherItem => {
        if (otherItem !== item) {
          otherItem.classList.remove('active');
        }
      });

      // Toggle current item
      item.classList.toggle('active');
    });
  });

  // Booking Modal
  const bookingModal = document.getElementById('bookingModal');
  const bookingForm = document.getElementById('bookingForm');
  const bookButtons = document.querySelectorAll('.book-service-btn');
  const modalClose = document.querySelector('.booking-modal__close');
  const modalCancel = document.querySelector('.booking-modal__cancel');
  const serviceSelect = document.getElementById('serviceType');

  // Open modal
  bookButtons.forEach(button => {
    button.addEventListener('click', function() {
      const service = this.dataset.service;
      bookingModal.classList.add('active');

      // Pre-select service if available
      if (service && serviceSelect) {
        serviceSelect.value = service;
      }
    });
  });

  // Close modal functions
  function closeModal() {
    bookingModal.classList.remove('active');
    bookingForm.reset();
  }

  modalClose.addEventListener('click', closeModal);
  modalCancel.addEventListener('click', closeModal);

  // Close on overlay click
  bookingModal.addEventListener('click', function(e) {
    if (e.target === bookingModal.querySelector('.booking-modal__overlay')) {
      closeModal();
    }
  });

  // Close on escape
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape' && bookingModal.classList.contains('active')) {
      closeModal();
    }
  });

  // Form submission
  bookingForm.addEventListener('submit', function(e) {
    e.preventDefault();

    // Here you would typically send the form data to your backend
    // For now, we'll just show a success message
    alert('Thank you for your booking request! We\'ll contact you within 24 hours to confirm your appointment.');

    closeModal();
  });

  // Set minimum date for booking (tomorrow)
  const tomorrow = new Date();
  tomorrow.setDate(tomorrow.getDate() + 1);
  const dateInput = document.getElementById('bookingDate');
  dateInput.min = tomorrow.toISOString().split('T')[0];
});
</script>
